for (var a = 0; a <= 5; a++) {
    // 自执行函数，每次循环都产生一个新的函数作用域，每个函数作用域存着这次循环的结果
    // 倒计时1秒后打印出每个函数作用域里面的值
    (function(a) {
      setTimeout(() => {
        console.log(a);
      }, 1000);
    })(a);
  }